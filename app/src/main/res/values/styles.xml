<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!--版本更新对话框弹窗动画-->
    <style name="updateDialogStyle" parent="android:Animation">
        <!--//进入时的动画-->
        <item name="android:windowEnterAnimation">@anim/dialog_enter</item>
        <!--//退出时的动画-->
        <item name="android:windowExitAnimation">@anim/dialog_exit</item>
    </style>
    <!-- Base application theme. -->
    <style name="BaseAppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimary</item>
        <item name="colorAccent">@color/colorAccent</item>

        <!-- 状态栏图标字体颜色为黑色-->
        <item name="android:statusBarColor">@color/colorPrimary</item>
        <item name="android:windowLightStatusBar">true</item>
        <!--底部导航栏-->
        <item name="android:navigationBarColor">@color/colorPrimary</item>
        <item name="android:windowLightNavigationBar" tools:ignore="NewApi">true</item>
        <!--阴影边界-->
        <item name="android:outlineProvider">background</item>
        <!--window背景-->
        <item name="android:windowBackground">@android:color/transparent</item>

        <item name="android:textAllCaps">false</item>
        <item name="android:textColorPrimary">@color/textColorPrimary</item>
        <item name="android:textColor">@color/textColorPrimary</item>
        <item name="android:editTextColor">@color/textColorPrimary</item>
        <item name="android:textColorSecondary">@color/textColorSecondary</item>
        <item name="colorControlHighlight">@color/colorRipple</item>
    </style>

    <style name="CustomThemeDialog" parent="@style/Theme.AppCompat.DayNight.NoActionBar">
        <!-- 去黑边
        <item name="android:windowFrame">@null</item>
        -->
        <!-- 设置是否可滑动
        <item name="android:windowIsFloating">false</item>
        -->
        <!-- 设置是否透明
        <item name="android:windowIsTranslucent">true</item>
         -->
        <!-- 无标题
        <item name="android:windowNoTitle">true</item>
        -->
        <!-- 背景
        <item name="android:background">@android:color/transparent</item>
        -->
        <!-- 窗口背景
        <item name="android:windowBackground">@android:color/transparent</item>
        -->
        <!-- 是否变暗 -->
        <item name="android:backgroundDimEnabled">true</item>
        <!-- 点击空白部分不消失
        <item name="android:windowCloseOnTouchOutside">false</item>
        -->
        <!-- 没有动画
        <item name="android:windowAnimationStyle">@style/updateDialogStyle</item>
        -->
    </style>

</resources>
